# 스타터팩 공통 환경변수 예시 파일이다.
# 개발/배포 모두 이 키를 기준으로 실제 .env를 만든다.
# 포트, 경로, 외부 연동 키는 프로젝트에 맞게 덮어쓴다.

# 개발/배포 모두 이 키를 기준으로 실제 .env를 만든다.
# 개발 환경은 development, 배포 환경은 production으로 설정한다.
APP_ENV=development
TZ=Asia/Seoul
PORTAL_HTTP_PORT=8080
BACKEND_PORT=8000
FRONTEND_PORT=5173

PROJECT_NAME=
# VITE_BASE_PATH는 /PROJECT_NAME 로 설정한다.
# 예시: PROJECT_NAME=govpage -> /govpage
VITE_BASE_PATH=

# 데이터 저장 경로
DATA_ROOT=/data
DATA_WORK=/data/work
# 데이터베이스 저장 경로
DATA_DB=/data/db
# 로그 저장 경로
DATA_LOGS=/data/logs

# LLM/검색 연동 키 placeholder
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=
QDRANT_URL=
QDRANT_API_KEY=

# 추후 DB/캐시 연동 placeholder
DB_HOST=
DB_PORT=5432
DB_NAME=
DB_USER=
DB_PASSWORD=
REDIS_HOST=
REDIS_PORT=6379
